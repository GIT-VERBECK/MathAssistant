# Configuration du serveur
HOST=0.0.0.0
PORT=5000
DEBUG=false

# CORS (origins séparés par des virgules)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Taille maximale d'upload (en bytes, par défaut 10MB)
MAX_UPLOAD_SIZE=10485760

# ===========================================
# API Keys - Remplissez avec vos clés
# ===========================================

# Mathpix API (OPTIONNEL - pour extraction LaTeX depuis images)
# Si non configuré, OpenAI Vision sera utilisé à la place
# Obtenez vos clés sur: https://mathpix.com/
MATHPIX_APP_ID=
MATHPIX_APP_KEY=

# WolframAlpha API (REQUIS - pour résolution de problèmes mathématiques)
# Obtenez votre App ID sur: https://products.wolframalpha.com/api/
WOLFRAM_APP_ID=your_wolfram_app_id

# LLM Configuration
# Choisissez entre "openai" ou "gemini"
LLM_PROVIDER=openai

# OpenAI (REQUIS - pour extraction LaTeX si Mathpix non configuré, et pour explications)
# Obtenez votre clé sur: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4o-mini

# Google Gemini (si LLM_PROVIDER=gemini)
# Obtenez votre clé sur: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

